export interface HealthCheckProps {
    apiName: string;
}
export class HealthCheck {
    // needs a route53 record set to be attached to
    // needs a lambda function for the more advanced health checks
}